__pycache__/
*.py[cod]
OUTPUT*
FIGURE
# we'll probably put FIGURE in eventually once we have some example figs
changelog
truncation_experiments.pkl
etc/calibration_runs/
# DASH N-body library (615MB archive + extracted runs); keep dash_index.pkl
etc/dash/DASH_19.05.09/
etc/dash/*.tar.gz
etc/dash/*.joblib
# Aguirre-Santaella+25 N-body archive + any extracted runs; keep README.md
etc/aguirre25_nbody/*.zip
etc/aguirre25_nbody/tidal_tracks_subhaloes/
.vscode/
*.code-workspace
slurm/
.venv/
# Galacticus comparison data (large hdf5) + per-run cache
analysis/galacticus_subhalos.hdf5
analysis/galacticus_runs_cache.pkl

# macOS Spotlight marker
.metadata_never_index
